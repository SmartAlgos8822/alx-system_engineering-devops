#!/bin/bash

# Delete the file betty
rm /tmp/betty

# Check if the file was deleted successfully
if [ $? -eq 0 ]; then
    echo "File 'betty' deleted successfully"
else
    echo "Failed to delete the file"
fi
